import JobContainer from '@/components/job/jobContainer';
import Layout from '@/components/layout';

const Job = () => {
  return (
    <Layout title="Workeé">
      <JobContainer />
    </Layout>
  );
};

export default Job;
